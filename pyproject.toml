[project]
name = "jtmethtools"
version = "0.1"
description = "Tools & data structures for methylation data."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "loguru>=0.7.2",
    "pysam>=0.22.1",
    "pandas>=2",
    "attrs"
]
